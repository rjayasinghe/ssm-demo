##
## Registration Service DB changelog.
##
## Always name changes "${nn}-changelog-${version}-${comment}.yaml" where
## `nn` is the change serial, for example '03', and `version` is the
## project artifact version (patch number may be 'x') where the change
## occurred.
##
##
databaseChangeLog:
  - include:
    - file: db/changelog/00-changelog-0.1.x-add-statemachine.yaml
  - include:
    - file: db/changelog/01-changelog-0.1.x-add-registration.yaml
